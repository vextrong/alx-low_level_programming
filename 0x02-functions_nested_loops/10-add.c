#include <stdio.h>
#include "main.h"
/**
 * add - adds two integers and returns the result
 * @a: first integer
 * @b: second integer
 * Return: sum of integers
 */
int add(int a, int b)
{
	return (a + b);
}
